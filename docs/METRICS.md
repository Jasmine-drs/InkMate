# InkMate 质量指标

**最后更新**: 2026-03-01
**基线版本**: 0.5.0

---

## 当前质量概览

| 指标 | 当前值 | 目标值 | 状态 |
|------|--------|--------|------|
| 后端测试覆盖率 | 待测量 | > 70% | ⚠️ 未测量 |
| 前端测试覆盖率 | 待测量 | > 60% | ⚠️ 未测量 |
| 构建时长（后端） | ~5s | < 10s | ✅ 达标 |
| 构建时长（前端） | ~7s | < 30s | ✅ 达标 |
| API 响应时间 (p95) | 待测量 | < 200ms | ⚠️ 未测量 |
| 页面加载时间 | 待测量 | < 3s | ⚠️ 未测量 |

---

## 第三阶段验收状态

### 构建验证
| 检查项 | 验证方式 | 状态 |
|--------|----------|------|
| 后端构建 | `python -m py_compile main.py` | ✅ 通过 |
| 前端构建 | `npm run build` | ✅ 通过 |
| TypeScript 检查 | `tsc --noEmit` | ✅ 通过 |

### 功能完成度 (SET-001 世界观设定)
| 功能模块 | 完成度 | 验收状态 |
|----------|--------|----------|
| 设定编辑页面 | 100% | ✅ 验收通过 |
| 加载状态反馈 | 100% | ✅ 验收通过 |
| 错误处理 | 100% | ✅ 验收通过 |
| 离开页面确认 | 100% | ✅ 验收通过 |
| 快捷键支持 | 100% | ✅ 验收通过 |
| 响应式设计 | 100% | ✅ 验收通过 |
| 表单验证 | 100% | ✅ 验收通过 |
| 代码审查 | 100% | ✅ 验收通过 |

### 功能完成度 (SET-002 角色卡管理)
| 功能模块 | 完成度 | 验收状态 |
|----------|--------|----------|
| 角色列表展示 | 100% | ✅ 验收通过 |
| 搜索和筛选 | 100% | ✅ 验收通过 |
| 角色创建/编辑 | 100% | ✅ 验收通过 |
| 角色删除 | 100% | ✅ 验收通过 |
| 角色卡详情编辑 | 100% | ✅ 验收通过 |
| 加载状态反馈 | 100% | ✅ 验收通过 |
| 离开页面确认 | 100% | ✅ 验收通过 |
| 响应式设计 | 100% | ✅ 验收通过 |
| 代码审查 | 100% | ✅ 验收通过 |

---

## 后端指标

### 构建指标
| 指标 | 测量值 | 目标 | 测量方式 |
|------|--------|------|----------|
| 启动时间 | ~5s | < 10s | `time uvicorn main:app` |
| 依赖安装时间 | ~30s | < 60s | `time pip install -r requirements.txt` |

### 代码质量
| 指标 | 测量值 | 目标 | 测量方式 |
|------|--------|------|----------|
| 测试覆盖率 | TBD | > 70% | `pytest --cov=backend` |
| 类型检查 | TBD | 100% | `mypy backend/` |
| Lint 错误数 | 0 | 0 | `flake8 backend/` |

### API 性能
| 端点 | p50 | p95 | p99 | 目标 |
|------|-----|-----|-----|------|
| GET /api/projects | TBD | < 100ms | < 200ms | p95 < 200ms |
| POST /api/projects | TBD | < 200ms | < 500ms | p95 < 500ms |
| GET /api/projects/{id}/chapters | TBD | < 150ms | < 300ms | p95 < 300ms |

---

## 前端指标

### 构建指标
| 指标 | 测量值 | 目标 | 测量方式 |
|------|--------|------|----------|
| 开发服务器启动 | ~2s | < 5s | `time npm run dev` |
| 生产构建 | ~15s | < 30s | `time npm run build` |
| 构建产物大小 | TBD | < 2MB | `npm run build` 输出 |

### 性能指标
| 指标 | 测量值 | 目标 | 测量方式 |
|------|--------|------|----------|
| FCP (First Contentful Paint) | TBD | < 1.5s | Lighthouse |
| LCP (Largest Contentful Paint) | TBD | < 2.5s | Lighthouse |
| TTI (Time to Interactive) | TBD | < 3.5s | Lighthouse |
| TBT (Total Blocking Time) | TBD | < 200ms | Lighthouse |

### 代码质量
| 指标 | 测量值 | 目标 | 测量方式 |
|------|--------|------|----------|
| 测试覆盖率 | TBD | > 60% | `npm run test -- --coverage` |
| TypeScript 错误数 | 0 | 0 | `tsc --noEmit` |
| ESLint 错误数 | 0 | 0 | `npm run lint` |

---

## 数据库指标

### 查询性能
| 查询类型 | 平均时间 | 目标 | 测量方式 |
|----------|----------|------|----------|
| 项目列表查询 | TBD | < 50ms | EXPLAIN + slow query log |
| 章节列表查询 | TBD | < 50ms | EXPLAIN + slow query log |
| 复杂关联查询 | TBD | < 100ms | EXPLAIN + slow query log |

### 连接池
| 指标 | 当前配置 | 建议值 |
|------|----------|--------|
| pool_size | 10 | 10-20 |
| max_overflow | 20 | 10-30 |

---

## 测试清单

### 后端测试
```bash
# 运行所有测试
pytest backend/ -v

# 覆盖率报告
pytest backend/ --cov=backend --cov-report=html

# 特定模块测试
pytest backend/routers/ -v
pytest backend/services/ -v
pytest backend/models/ -v
```

### 前端测试
```bash
# 运行所有测试
npm run test

# 覆盖率报告
npm run test -- --coverage

# 构建检查
npm run build
```

### 端到端测试
```bash
# 启动服务后运行
# TBD: 配置 Playwright 或 Cypress
```

---

## 持续集成检查清单

### 每次提交必须通过
- [ ] 后端构建 (`cd backend && python -m py_compile main.py`)（注意直接运行python会失败，当前环境是wsl，使用的是conda的虚拟环境在D:\code\data\miniconda\envs\InkMate）后端不需要测试运行，后端有热重载更新
- [ ] 前端构建 (`cd frontend && npm run build`)
- [ ] 后端 Lint (`flake8 backend/`)
- [ ] 前端 Lint (`npm run lint`)
- [ ] TypeScript 检查 (`tsc --noEmit`)

### 合并请求必须通过
- [ ] 所有单元测试通过
- [ ] 代码覆盖率不下降
- [ ] 人工代码审查通过

---

## 性能基线

### 基准测试场景
1. 创建 100 个项目
2. 每个项目创建 50 个章节
3. 查询项目列表（分页）
4. 查询章节列表（分页）
5. 创建新章节

### 基线数据（待填充）
| 场景 | 操作/s | 错误率 | 目标 |
|------|--------|--------|------|
| 创建项目 | TBD | < 0.1% | > 10/s |
| 创建章节 | TBD | < 0.1% | > 20/s |
| 查询项目列表 | TBD | < 0.1% | > 100/s |
| 查询章节列表 | TBD | < 0.1% | > 50/s |

---

## 质量趋势

### 第 1 周 (2026-02-24 ~ 2026-02-28)
| 指标 | 周一 | 周二 | 周三 | 周四 | 周五 | 趋势 |
|------|------|------|------|------|------|------|
| 后端 API 数量 | 3 | 6 | 15 | 25 | 30 | 📈 |
| 前端页面数量 | 1 | 2 | 3 | 3 | 4 | 📈 |
| Bug 数量 | - | - | - | - | - | - |

---

## 改进计划

### 短期（下周）
1. 配置 pytest 覆盖率报告
2. 配置前端 Lighthouse 测试
3. 建立 API 性能基准测试

### 中期（本月）
1. 配置 CI/CD 流水线
2. 实现自动化 E2E 测试
3. 配置性能监控告警

### 长期（本季度）
1. 实现性能自动化回归检测
2. 建立用户体验监控体系
3. 实现灰度发布能力

---

## 备注

1. 所有「TBD」指标需要在下一阶段完成测量
2. 性能基线测试需要在真实环境中运行
3. 测试覆盖率目标可根据实际情况调整
